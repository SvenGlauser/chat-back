spring:
  datasource:
    url: jdbc:h2:mem:chat
    username: sa
    password:
    driverClassName: org.h2.Driver

  batch:
    job:
      enabled: false

chat:
  auth-server-url: http://localhost:8081/
  realm: chat-keycloak
  administration:
    realm: master
    resource: admin-cli
    user: admin
    password: admin